import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()(() => ({
    userCard: {
        display: 'flex',
        flexDirection: 'column',
        cursor: 'pointer',
    },
}));
